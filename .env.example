# --- MODE ---
# If true, the app uses demo data (no DB required).
DEMO_MODE=true

# --- DATABASE (for Prisma/Postgres) ---
# Example Neon Postgres URL (replace with your own if DEMO_MODE=false)
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require"
DATABASE_URL=

# --- APP ---
NEXT_PUBLIC_APP_NAME="Trade2Earn"
